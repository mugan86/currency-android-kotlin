package amldev.currency.data.db

import domain.model.Money

/**
 * Created by anartzmugika on 22/8/17.
 */
class DbDataMapper() {

    fun convertFromDomain(moneys: Money) = with(moneys) {
        // MoneyInfoTable(symbol, name, icon)
    }

}